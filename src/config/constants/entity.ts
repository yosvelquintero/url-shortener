import { DeepReadonly } from 'ts-essentials';

import { IEntity } from '@app/types/index';

export const ENTITY: DeepReadonly<IEntity> = {
  names: {
    urlEntity: 'UrlEntity',
    userEntity: 'UserEntity',
  },
};
